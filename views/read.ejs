<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheet/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="main w-full min-h-screen bg-zinc-900 p-10 text-white">
       
    <h1 class="text-3xl"> All user</h1>
    <div>
        <a href="/" class="text-red-600 text-3xl">go back </a>
    </div>
<div class="users flex gap-3 flex-wrap">
    <div class="user max-w-72 p-10 bg-zinc-400 rounded-xl">
        <% allusers.forEach((user)=>{%>
            <h3 class="text-xl tracking-tighter"><%= user.name%></h3>
    <h5 class="text-blue-900"><%= user.email%></h5>

          

    <div class="flex gap-10 mt-3">
 <a  class="text-green-600 text-4xl" href="/edit/<%= user._id%>">edit</a>
 <a class="text-red-800 text-4xl"  href="/delete/<%= user._id%>">delete</a>
    </div>
    <%  })%>
    </div>

</div>
</div>
</body>
</html>